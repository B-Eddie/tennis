{
  "name": "phone-tennis",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n GAME,PHONE -c green,magenta \"npm run dev -w game\" \"npm run dev -w phone\"",
    "build": "npm run build -w game && npm run build -w phone"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
